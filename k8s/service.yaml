apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  namespace: dev

spec:
  type: LoadBalancer
  selector:
    app: flask-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP